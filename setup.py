from setuptools import setup, find_packages
from codecs import open
from os import path

here = path.abspath( path.dirname( __file__ ) )

setup(
    name='clima-api',
    version='1.0.0',
    description='API de clima e cadastro',
    license='Apache-2.0'
)
